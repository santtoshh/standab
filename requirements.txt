# Fully pinned to the known-good local environment.
# Exact pins prevent pip's resolver from backtracking (which previously
# downgraded ipython/matplotlib to ancient source builds and failed), and
# lock the Jupyter stack to a version that does NOT import the removed
# 'ipython_genutils' module (which had killed the kernel on /generate).
flask==3.1.3
werkzeug==3.1.8
gunicorn==26.0.0
folium==0.20.0
numpy==2.2.6
pandas==2.3.3
openpyxl==3.1.5
matplotlib==3.10.8
ipython==8.38.0
ipykernel==7.1.0
jupyter_client==8.8.0
jupyter_core==5.9.1
traitlets==5.14.3
pyzmq==27.1.0
nbformat==5.10.4
nbclient==0.10.4
h3==4.4.1
scipy==1.15.3
